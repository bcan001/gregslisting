<div class="span4 well" style="text-align:center;background:white;border:white;">
</div>
<div class="span8 well" style="background:white;border:white;">

			<h3 style="text-align:center;"><%= Category.find(params[:id]).name %></h3>

			<% @listings.each do |l| %>
				<p><%= l.created_at %>: <%= link_to l.title, listing_path(l) %>: <%= l.city %>, <%= l.state %> in <%= l.subcategory.name %></p>
			<% end %>

    </div>
</div>
<div class="span4 well" style="text-align:center;background:white;border:white;">
</div>